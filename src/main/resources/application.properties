
spring.datasource.url=jdbc:mysql://localhost:3306/humosoft_cnpm?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=10112004
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# HikariCP Configuration
# Kích thước tối đa của pool kết nối
spring.datasource.hikari.maximum-pool-size=10
# Số kết nối tối thiểu trong pool
spring.datasource.hikari.minimum-idle=5
# Thời gian tối đa giữ kết nối không hoạt động (30s)
spring.datasource.hikari.idle-timeout=30000
# Thời gian tối đa tồn tại của một kết nối (30 phút)
spring.datasource.hikari.max-lifetime=1800000

spring.datasource.hikari.connection-timeout=30000

# ===============================
# JPA / HIBERNATE CONFIGURATION
# ===============================
# "update": cập nhật schema, "validate": kiểm tra, "create": tạo mới (mất dữ liệu cũ)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.show-sql=true

# ===============================
# EMAIL CONFIGURATION (GMAIL)
# ===============================
spring.mail.host=smtp.gmail.com
# Địa chỉ gửi email (Dùng biến môi trường MAIL_FROM hoặc mặc định)
spring.mail.from=${MAIL_FROM:vunguyen10112k4@gmail.com}
spring.mail.port=587
spring.mail.username=vunguyen10112k4
# Mật khẩu ứng dụng của Gmail
spring.mail.password=wwhh bopv lzll wxlf

# SMTP Properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ===============================
# SECURITY / JWT CONFIGURATION
# ===============================
# Lưu ý: Dựa trên indentation của bạn, phần này nằm dưới prefix "spring"
spring.security.jwt.secret-key=Ewu4H0jzCNxXtoJS1D9YzLZ/zENfOLLdYQIudk+P5BLa66bFbXt2D/to55MViGnF
# Thời gian hết hạn token (1 ngày)
spring.security.jwt.expiration=86400000
# Thời gian hết hạn refresh token (7 ngày)
spring.security.jwt.refresh-token.expiration=604800000